name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm run test

      - name: Production build
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://qeahcvudoctlagonfqmz.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFlYWhjdnVkb2N0bGFnb25mcW16Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODEwNjU3MDcsImV4cCI6MjA5NjY0MTcwN30.1KPpULXvjSsenwNdcr50LOeRLF-I6CRGbVc4mNXCu4A
          NEXT_PUBLIC_SITE_URL: https://lead-generator-agent-iota.vercel.app
          COMPANY_DATA_PROVIDER: scraping
          EMAIL_VERIFICATION_PROVIDER: dns
